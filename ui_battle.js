// ui_battle.js
window._X_UI = window._X_UI || {}; // 如果沒有就建立一個 UI 總管物件

_X_UI.battle = {
    updateHP: function(val) { 
        // 這裡放原本更新血條的代碼
        console.log("血條更新中...");
    },
    log: function(msg) {
        // 這裡放原本噴戰鬥日誌的代碼
    }
};
